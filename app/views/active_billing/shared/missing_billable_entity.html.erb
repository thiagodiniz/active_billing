<h1><%= t("active_billing.errors.missing_billable_entity.title", default: "Billable entity required") %></h1>
<p>
  <%= t("active_billing.errors.missing_billable_entity.body",
        default: "This page requires a billable_entity_id (and billable_entity_type unless a default is configured). Add ?billable_entity_id=...&billable_entity_type=... to the URL.") %>
</p>
